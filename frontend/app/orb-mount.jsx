'use client';
import {useEffect,useRef,useState} from 'react';import {createPortal} from 'react-dom';import {Canvas,useFrame} from '@react-three/fiber';
function Orb(){const ref=useRef();useFrame((s,d)=>{ref.current.rotation.y+=d*.42;ref.current.rotation.x=Math.sin(s.clock.elapsedTime*.55)*.12});return <group ref={ref}><mesh><sphereGeometry args={[1.18,64,64]}/><meshStandardMaterial color="#ad4656" roughness={.22} metalness={.35} emissive="#3b102c" emissiveIntensity={.4}/></mesh><mesh rotation={[Math.PI/2,0,0]}><torusGeometry args={[1.7,.012,8,96]}/><meshBasicMaterial color="#e7bf79" transparent opacity={.72}/></mesh><mesh rotation={[.45,.8,.2]}><torusGeometry args={[1.98,.008,8,96]}/><meshBasicMaterial color="#ee795d" transparent opacity={.45}/></mesh></group>}
function Scene(){return <Canvas dpr={[1,2]} camera={{position:[0,0,4.9],fov:42}} gl={{powerPreference:'high-performance'}}><ambientLight intensity={1.2}/><pointLight position={[2.5,2,3]} intensity={24} color="#ffd0a3"/><pointLight position={[-3,-1,1]} intensity={11} color="#9c3d73"/><Orb/></Canvas>}
export default function OrbMount(){const [target,setTarget]=useState();useEffect(()=>{setTarget(document.querySelector('.signal-wrap'))},[]);return target?createPortal(<div className="webgl-signal" aria-hidden="true"><Scene/></div>,target):null}
