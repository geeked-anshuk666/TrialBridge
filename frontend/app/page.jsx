'use client';
import {useState} from 'react';
export default function Home(){const [text,setText]=useState('');const [data,setData]=useState(null);const [busy,setBusy]=useState(false);async function search(){setBusy(true);try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000'}/api/search`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({patient_description:text})});setData(await r.json())}finally{setBusy(false)}}return <main><section className="hero"><nav><span className="brand">TRIAL<span>BRIDGE</span></span><span className="pill">Private by design · Free to explore</span></nav><div className="hero-grid"><div><p className="kicker">A gentler way to find possibilities</p><h1>Clinical trials,<br/><i>in your language.</i></h1><p className="lead">Describe what you’re going through. TrialBridge helps you discover studies worth discussing with your doctor.</p><div className="searchbox"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Tell us what you’re experiencing…" aria-label="Describe your condition"/><button disabled={!text||busy} onClick={search}>{busy?'Searching…':'Find my possibilities →'}</button></div><p className="hint">Your words stay private. No account required.</p></div><div className="orb-wrap" aria-hidden="true"><div className="orb"><div className="orb-core"/><div className="orbit orbit-a"/><div className="orbit orbit-b"/><span className="orb-label">POSSIBILITY</span></div></div></div></section>{data&&<section className="results"><p className="kicker">{data.detected_language==='en'?'Your possibilities':'Detected language: '+data.detected_language}</p><h2>A few places to begin</h2>{data.explained_trials?.map(t=><article className="card" key={t.trial_id}><div><h3>{t.title}</h3><p>{t.explanation}</p><small>{t.location_plain}</small></div><div className="score"><strong>{t.eligibility_score}</strong><span>potential<br/>match</span></div></article>)}{!data.explained_trials?.length&&<p>We didn’t find a close match yet. Try broader words and check back later.</p>}<p className="disclaimer">TrialBridge is an informational discovery tool, not a clinical eligibility decision. Please discuss every result with a qualified healthcare professional.</p></section>}</main>}
