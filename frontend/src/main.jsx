import React, {useState} from 'react';
import {createRoot} from 'react-dom/client';
import './styles.css';

function App(){const [text,setText]=useState('');const [data,setData]=useState(null);const [busy,setBusy]=useState(false);const search=async()=>{setBusy(true);const r=await fetch('http://localhost:8000/api/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({patient_description:text})});setData(await r.json());setBusy(false)};return <main><header><p className="eyebrow">TRIALBRIDGE</p><h1>Find trials that speak your language.</h1><p className="intro">Describe your condition in your own words. We’ll help you discover possibilities to discuss with your doctor.</p></header><section className="search"><textarea aria-label="Condition description" value={text} onChange={e=>setText(e.target.value)} placeholder="Tell us what you’re experiencing…"/><button onClick={search} disabled={!text||busy}>{busy?'Searching…':'Search trials'}</button></section>{data&&<section><p className="language">Detected language: {data.detected_language}</p>{data.explained_trials.map(t=><article className="card" key={t.trial_id}><div><h2>{t.title}</h2><p>{t.explanation}</p><small>{t.location_plain}</small></div><span className="badge">{t.eligibility_score}<br/><em>potential match</em></span></article>)}{!data.explained_trials.length&&<p>No matches yet. Try broader terms or check back later.</p>}<p className="disclaimer">Informational discovery only—not a clinical eligibility determination. Discuss every result with a qualified healthcare professional.</p></section>}</main>}
createRoot(document.getElementById('root')).render(<App/>);

