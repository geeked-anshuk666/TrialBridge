# Troubleshooting
Registry timeout: inspect correlation ID and the `registries_responded` list; a missing registry is partial failure. Genuine zero results has all queried registries represented but `explained_trials` empty. Language fallback uses English unless Devanagari is detected or an override is supplied.

