# API reference
`POST /api/search` accepts `{patient_description, override_language?}` and returns profile, registry transparency, six explained trials, and a correlation ID. `POST /api/notify/subscribe` stores only keywords, language, and Web Push material. `GET /health` is the liveness probe.

