# Testing strategy
Add unit tests for profile extraction and notification diffing, plus mocked `/api/search` integration tests in English and Hindi. The current health endpoint and API contracts are smoke-testable with FastAPI TestClient.

