# System overview
TrialBridge accepts multilingual free text, extracts a minimal profile, queries registries in parallel, ranks candidates, and returns patient-language explanations. The UI always carries the detected language; the physician report remains English.

