# Known tradeoffs
The MVP uses three registries rather than all WHO ICTRP sources, SQLite rather than Postgres, and a minimal UI. Production paths are documented in the deployment and scaling guides.

