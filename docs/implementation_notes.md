# Implementation notes
Prompt boundaries are reserved for versioned `PROFILE_PROMPT`, `SCORE_PROMPT`, and `EXPLAIN_PROMPT` constants. Runtime outputs must be JSON validated and retried once before a user-facing error.

